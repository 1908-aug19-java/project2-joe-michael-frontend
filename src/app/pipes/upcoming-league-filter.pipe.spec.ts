import { UpcomingLeagueFilterPipe } from './upcoming-league-filter.pipe';

describe('UpcomingLeagueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingLeagueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FantasyFilterPipe } from './fantasy-filter.pipe';

describe('FantasyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FantasyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

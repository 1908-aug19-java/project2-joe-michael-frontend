import { MatchesFilterPipe } from './matches-filter.pipe';

describe('MatchesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
